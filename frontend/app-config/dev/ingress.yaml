apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: inventoryjs-frontend-ingress
  namespace: inventoryjs-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  tls:
    - host:
        - inventory.findmyapp.net
      secretName: inventory-findmyapp-staging-tls
  #  tls:
  #  - hosts:
  #      - inventory.findmyapp.net
  #    secretName: findmyapp-net-staging-tls
  rules:
    - host: "inventory.findmyapp.net"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 80
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: inventoryjs2-frontend-ingress
  namespace: inventoryjs-frontend
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-staging
spec:
  tls:
    - host:
        - inventory2.findmyapp.net
      secretName: inventory2-findmyapp-staging-tls
  rules:
    - host: "inventory2.findmyapp.net"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: frontend-service
                port:
                  number: 80
